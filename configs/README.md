# Configurations

- `models/`: reusable model provenance and inference settings.
- `benchmarks/`: reusable dataset, filtering, score-normalization, and metric policies.

Experiment-specific choices belong in `experiments/<experiment-id>/experiment.yaml`.

