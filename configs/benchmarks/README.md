# Benchmark Configurations

Each benchmark configuration should specify:

- dataset source, version, license, and checksum;
- canonical manifest;
- inclusion and exclusion rules;
- experimental measurement type, units, and qualifier policy;
- prediction head to evaluate;
- metric definitions and aggregation policy;
- bootstrap unit and random seed;
- leakage, temporal, and chemical-novelty limitations.

