# Experiments

Each experiment gets a stable directory such as:

```text
experiments/exp001_boltz2_smoke/
├── README.md
└── experiment.yaml
```

The README explains the scientific question and interpretation boundaries. The YAML is the machine-readable protocol. Copy `_template/` when defining an experiment.

