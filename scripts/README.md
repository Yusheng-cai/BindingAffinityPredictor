# Scripts

Scripts should be thin operational entry points that call reusable package code. Avoid embedding scientific constants, label transformations, or benchmark-specific filters only in shell scripts.

