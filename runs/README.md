# Local Run Artifacts

Raw predictions, generated structures, logs, and run metadata are written beneath this directory and ignored by Git.

See `docs/agentic-workflow.md` for the canonical run layout.

